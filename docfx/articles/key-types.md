# Key Types

The generated API reference includes the most useful entry points for consumers of the library.

## Main Entry Points

- `OverkizClient` — the primary client class for cloud and local API communication

## Common Supporting Types

- `OverkizServer` — enumeration of supported cloud server endpoints
- `Device` — represents a discovered smart-home device
- `DeviceState` — represents the current state of a device
- `Command` — represents an executable device command

## Full Reference

For the full type list, use the generated [API Reference](../api/toc.yml).
